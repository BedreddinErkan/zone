{
  "tasks": [
    {
      "id": "T1-comment-add",
      "tier_expected": "simple",
      "description": "Add a single-line comment '// Zone API entry point' directly above the first export statement in src/cli/index.ts",
      "expected_max_cost_usd": 0.05,
      "expected_rolled_back": false,
      "notes": "Minimal single-file, single-line change — canonical simple tier smoke"
    },
    {
      "id": "T2-single-file-rename",
      "tier_expected": "simple",
      "description": "In src/utils/logger.ts, rename the local helper function 'truncate' to 'truncateString' and update all calls to it within the same file",
      "expected_max_cost_usd": 0.05,
      "expected_rolled_back": false,
      "notes": "Single-file rename with call-site updates — still simple tier"
    },
    {
      "id": "T3-two-file-addition",
      "tier_expected": "medium",
      "description": "In src/utils/logger.ts, add and export a new function at the end of the file: `export function logJson(tag: string, payload: Record<string, unknown>): void { log(tag, JSON.stringify(payload)); }`. Then in src/llm/taskClassifier.ts, add `logJson` to the existing import from '../utils/logger.js' (which already imports `log`) and replace all calls of the form `log('[zone-task-classified]', JSON.stringify({...}))` with `logJson('[zone-task-classified]', {...})` — remove the JSON.stringify wrapper when switching to logJson. Leave all other log calls (for '[zone-task-classifier-failure]' etc.) unchanged.",
      "expected_max_cost_usd": 0.20,
      "expected_rolled_back": false,
      "notes": "Two-file addition: new logJson export in logger.ts, used in taskClassifier.ts — stable target, no extractions"
    },
    {
      "id": "T4-direct-typecheck-break",
      "tier_expected": "simple",
      "description": "Create the file src/utils/typecheck-probe.ts with EXACTLY this content and no other changes:\n\n// Zone L.4 sweep probe — intentional type errors for rollback testing.\n// DO NOT fix the type mismatches below. They exist to test Zone's staged\n// typecheck verification and J.3 atomic rollback pipeline.\nexport const brokenNumber: number = 'this is a string';\nexport const brokenBool: boolean = 42;\nexport function brokenReturn(): string { return 123; }\n\nAll three assignments must remain type-incorrect. Do not change any type annotations or values to make them compatible. This file is a deliberate probe: Zone must detect the 3 typecheck regressions during staged verification and roll back the entire change automatically.",
      "expected_max_cost_usd": 0.05,
      "expected_rolled_back": true,
      "notes": "Deterministic 3-error typecheck break via new file — errors are self-contained, not codebase-dependent; verifies J.3 atomic rollback for staged type regressions"
    },
    {
      "id": "T5-tier-force-simple",
      "tier_expected": "complex",
      "tier_forced": "simple",
      "description": "In src/llm/taskClassifier.ts, change the return type of classifyTask so it returns a Promise<TaskClassification | null> instead of Promise<TaskClassification>. Update the function signature and return statements accordingly, but do NOT update callers.",
      "expected_max_cost_usd": 0.05,
      "expected_rolled_back": true,
      "notes": "Same complex task as old T4 but dispatched with ZONE_FORCE_TIER=simple — Task tool removed, subagents blocked. Tests tier constraint enforcement."
    }
  ]
}
