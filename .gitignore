node_modules

.env
node_modules/
dist/

# Environment files (allow only .env.example)
.env
.env.*
!.env.example
zone-debug.log
zone-debug.log.*
zone-debug.log
zone-debug.log.*
.claude/settings.local.json

# Runtime artifacts (verifyVisual screenshots, run-local config)
.zone/

# Sweep output (append-only CSV, not versioned)
data/sweep-results.csv

# Stray compiled output under scripts/ — no build step produces this; origin unknown,
# established during cleanup (see docs/deferred-work.md item 183).
scripts/*.js
scripts/*.js.map
scripts/*.d.ts
err.txt
out.txt
*.log
