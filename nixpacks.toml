[phases.setup]
providers = ["node"]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["ls -la src/ui/", "npm run build", "ls -la dist/ui/"]

[start]
cmd = "node dist/cli/index.js serve"