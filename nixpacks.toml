[phases.setup]
providers = ["node"]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/cli/index.js serve"

[phases.build]
cmds = ["npm run build", "ls -la dist/ui/"]